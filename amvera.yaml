version: 2
meta:
  name: contact-bot
deploy:
  type: python
  command: python app.py
  environment:
    - name: BOT_TOKEN
      value: ${BOT_TOKEN}
    - name: DATABASE_URL
      value: ${DATABASE_URL}